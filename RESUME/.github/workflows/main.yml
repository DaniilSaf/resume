name: Build Resume PDF

on:
  push:
    branches: [ main, master ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Build and Compile
        run: |
          docker build -t resume-builder .
          docker run --rm -v ${{ github.workspace }}/CV:/CV resume-builder
