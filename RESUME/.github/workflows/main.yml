name: Build Resume PDF

on:
  push:
    branches: [ main, master ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Build with Docker
        run: |
          docker build -t resume-builder .
          docker run --rm -v ${{ github.workspace }}:/workspace resume-builder
